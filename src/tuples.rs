// Group values of different types together
// Maximum 12 elements

pub fn run() {
    let person = ("John", "New York", 23);

    println!("{} is from {} and is {}!", person.0, person.1, person.2);
}
